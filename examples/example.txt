/*
This is my local commment
*/

global commands = 1000000

import "consts"

define square(x)
	switch x
		0: 
			return 0
		default:
			return x ** 2
	end
end

local x = square(32)

*("spr" .. x .. "_x") = consts.PLAYER_HEALTH

put c0F CustomBlock p?? at 100 200
